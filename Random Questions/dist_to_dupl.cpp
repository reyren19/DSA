/*1. Given an array of integers, print out an array where at each index i, the total distance from i to every other duplicate element of i in 
the array is shown. For example, if the array was [1,3,1,1,2], the answer would be: [5,0,3,4,0]. */

#include<bits/stdc++.h>
using namespace std;

int dist_to_dupl(int arr[]){
    
}

int main() {
int arr[] = {1,3,1,1,2};
return dist_to_dupl(arr);
}
